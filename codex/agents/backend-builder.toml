name = "backend_builder"
description = "Implements API, authentication and authorization, input validation, and business logic for a vertical feature."
model = "gpt-6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = "Follow the task card, ownership, allowed paths, and API contract. Change API, auth, validation, and business logic only; do not change frontend UI or database schema. Do not create subagents or reassign work. Run focused verification and report evidence."

[agents]
enabled = false
